#include <cmath>

class Point
{
public:
	Point(double x = 0, double y = 0);
private:
	double m_X, m_Y;
};

int main(void) {
	double a, r, x, y;
	Point p = (x + r * cos(a), y + r * sin(a));
	return 0;
}